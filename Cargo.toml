[package]
name = "cli-test"
version = "0.1.0"
edition = "2024"

[dependencies]
toml = "0.8"
tree-sitter-test = { path = "../tree-sitter-test" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
clap = { version = "4.0", features = ["derive"] }
